# Get JIRA ID:
BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD 2> /dev/null | grep -oE "[A-Z]+-[0-9]+")
COMMIT_MESSAGE=$(cat $1)
# If branch isn't empty and is a commit message type.
if [[ -n "$BRANCH_NAME" && "$2" = "message"* ]];
then
    printf "[$BRANCH_NAME] $COMMIT_MESSAGE" > $1
fi
